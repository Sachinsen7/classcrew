"use client";

import PaymentLayout from "@/components/My page/PaymentManagement/layout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <PaymentLayout>{children}</PaymentLayout>;
}

